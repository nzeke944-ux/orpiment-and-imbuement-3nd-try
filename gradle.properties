# Assumption: targeting Minecraft 26.2 (per user spec). This was generated against
# the most current Fabric 1.21.x-era API/patterns known at build time.
# The other AI compiling this should swap in the exact Yarn mappings / Fabric API
# version published for 26.2 if these values are stale.
minecraft_version=26.2
yarn_mappings=26.2+build.1
loader_version=0.16.10

mod_version=1.0.0
maven_group=com.orpiment
archives_base_name=orpimentimbuement

fabric_version=0.110.0+26.2

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
